name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff linter
        run: ruff check .

      - name: Run ruff formatter check
        run: ruff format --check .

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: "1.8.2"
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Install dependencies
        run: |
          cd agentform-schema && poetry install --no-interaction
          cd ../agentform-compiler && poetry install --no-interaction
          cd ../agentform-mcp && poetry install --no-interaction
          cd ../agentform-runtime && poetry install --no-interaction
          cd ../agentform-cli && poetry install --no-interaction

      - name: Run mypy on agentform-schema
        run: cd agentform-schema && poetry run mypy agentform_schema

      - name: Run mypy on agentform-compiler
        run: cd agentform-compiler && poetry run mypy agentform_compiler

      - name: Run mypy on agentform-mcp
        run: cd agentform-mcp && poetry run mypy agentform_mcp

      - name: Run mypy on agentform-runtime
        run: cd agentform-runtime && poetry run mypy agentform_runtime

      - name: Run mypy on agentform-cli
        run: cd agentform-cli && poetry run mypy agentform_cli

  test:
    name: Test (${{ matrix.package }} / Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
        package:
          - agentform-schema
          - agentform-compiler
          - agentform-mcp
          - agentform-runtime
          - agentform-cli

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: "1.8.2"
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: ${{ matrix.package }}/.venv
          key: venv-${{ runner.os }}-${{ matrix.python-version }}-${{ matrix.package }}-${{ hashFiles(format('{0}/poetry.lock', matrix.package)) }}
          restore-keys: |
            venv-${{ runner.os }}-${{ matrix.python-version }}-${{ matrix.package }}-

      - name: Install dependencies for agentform-schema
        if: matrix.package != 'agentform-schema'
        run: cd agentform-schema && poetry install --no-interaction

      - name: Install dependencies for agentform-compiler
        if: matrix.package == 'agentform-compiler' || matrix.package == 'agentform-runtime' || matrix.package == 'agentform-cli'
        run: cd agentform-compiler && poetry install --no-interaction

      - name: Install dependencies for agentform-mcp
        if: matrix.package == 'agentform-mcp' || matrix.package == 'agentform-runtime' || matrix.package == 'agentform-cli'
        run: cd agentform-mcp && poetry install --no-interaction

      - name: Install dependencies for agentform-runtime
        if: matrix.package == 'agentform-runtime' || matrix.package == 'agentform-cli'
        run: cd agentform-runtime && poetry install --no-interaction

      - name: Install package dependencies
        run: cd ${{ matrix.package }} && poetry install --no-interaction

      - name: Run tests with coverage
        run: |
          cd ${{ matrix.package }}
          poetry run pytest tests/ --cov=./ --cov-report=xml --cov-report=term-missing -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ${{ matrix.package }}/coverage.xml
          flags: ${{ matrix.package }}
          name: ${{ matrix.package }}-py${{ matrix.python-version }}
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

