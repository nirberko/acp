[tool.pytest.ini_options]
testpaths = [
    "agentform-schema/tests",
    "agentform-compiler/tests",
    "agentform-mcp/tests",
    "agentform-runtime/tests",
]
asyncio_mode = "auto"
addopts = "-v"
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

